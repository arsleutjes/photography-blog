# photography-blog

Lightroom export presets and watermarks for the photography blog workflow.

## Setup

Run `install-presets.ps1` to install the export and watermark presets into Lightroom.

## Backup

Run `backup.ps1` to mirror the source drive to the destination drive using robocopy.