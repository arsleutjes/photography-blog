# New pictures

- Create folder on per project in `D:/Pictures/Raw/`
  - Folder naming convention `yyyy-mm-dd-Project_Name`

# New videos

- Create folder per project in `D:/Videos/Raw/`
  - Folder naming convention `yyyy-mm-dd-Project_Name`

# Backup once per month

- See `./backup.bash`
